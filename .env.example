# Server
PORT=3000

# MongoDB — used only on first start to seed ~/.vee/config.json; delete that file to re-apply
MONGO_URI=mongodb://localhost:27017
MONGO_DB=vee

# Chat context
SYSTEM_PROMPT="You are a helpful assistant."
TOKEN_LIMIT=4096
